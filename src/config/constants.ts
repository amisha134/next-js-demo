export const AUTH = {
  COOKIE_TOKEN: 'rm-auth',
}

export const ROUTES = {
  AUTH_LOGIN: '/auth/login',
  HOME: '/',
}

export const API_ROUTES = {
  SHOW: 'https://dummyjson.com/products',
  BE: '/api',
}
